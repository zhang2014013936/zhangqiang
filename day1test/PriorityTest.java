//package day1test;

import javax.swing.*;

/*public class PriorityTest extends JFrame{
    public PriorityTest(){
        JProgressBar processBar;
        Thread threadA =new Thread(new MyThread(processBar));
    }
    public static void setPriority(String threadName,int priority,Thread t){
        t.setPriority(priority);
        t.setName(threadName);
        t.start();

    }
    private final class MyThread implements Runnable{
        private final JProgressBar bar;
        int count =0;
        private MyThread(JProgressBar bar) {
            this.bar = bar;
        }
        public void run(){
            while (true){
                bar.setValue(count+=10);
                try{
                    Thread.sleep(1000);
        } catch (InterruptedException e) {
                    e.printStackTrace();
                }
            }
        }

        }
    }*/


